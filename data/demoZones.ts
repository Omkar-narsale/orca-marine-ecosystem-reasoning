/**
 * DEPRECATED — Production code does not import demo zones.
 * Retained as empty array for backward compatibility with isolated legacy test fixtures.
 */
import { MarineZone } from '@/types/marine';

export const DEMO_ZONES: MarineZone[] = [];
